void main() {
  runApp(const FootballApp());
}

class FootballApp extends StatelessWidget {
  const FootballApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Football Hub',
      debugShowCheckedModeBanner: false,
      theme: ThemeData(
        brightness: Brightness.dark,
        primarySwatch: Colors.purple,
      ),
      home: const CompetitionPage(),
    );
  }
}

class CompetitionPage extends StatelessWidget {
  const CompetitionPage({super.key});

  @override
  Widget build(BuildContext context) {
    final leagues = [
      {
        'id': 'epl',
        'name': 'Premier League',
        'country': 'Inggris',
        'icon': '🇬🇧',
        'colors': [Color(0xFF9333EA), Color(0xFFEC4899)],
      },
      {
        'id': 'laliga',
        'name': 'La Liga',
        'country': 'Spanyol',
        'icon': '🇪🇸',
        'colors': [Color(0xFFF97316), Color(0xFFEF4444)],
      },
      {
        'id': 'seriea',
        'name': 'Serie A',
        'country': 'Italia',
        'icon': '🇮🇹',
        'colors': [Color(0xFF2563EB), Color(0xFF06B6D4)],
      },
      {
        'id': 'bundesliga',
        'name': 'Bundesliga',
        'country': 'Jerman',
        'icon': '🇩🇪',
        'colors': [Color(0xFFDC2626), Color(0xFFEAB308)],
      },
      {
        'id': 'ligue1',
        'name': 'Ligue 1',
        'country': 'Prancis',
        'icon': '🇫🇷',
        'colors': [Color(0xFF1D4ED8), Color(0xFF3B82F6)],
      },
    ];

    return Scaffold(
      body: Container(
        decoration: BoxDecoration(
          gradient: LinearGradient(
            begin: Alignment.topLeft,
            end: Alignment.bottomRight,
            colors: [
              Color(0xFF0F172A),
              Color(0xFF581C87),
              Color(0xFF0F172A),
            ],
          ),
        ),
        child: SafeArea(
          child: Column(
            children: [
              // Header
              Container(
                decoration: BoxDecoration(
                  color: Colors.black.withOpacity(0.4),
                  border: Border(
                    bottom: BorderSide(
                      color: Colors.white.withOpacity(0.1),
                      width: 1,
                    ),
                  ),
                ),
                padding: const EdgeInsets.all(24),
                child: Row(
                  children: [
                    // Back Button
                    GestureDetector(
                      onTap: () {
                        Navigator.pop(context);
                      },
                      child: Container(
                        width: 40,
                        height: 40,
                        decoration: BoxDecoration(
                          color: Colors.white.withOpacity(0.1),
                          borderRadius: BorderRadius.circular(12),
                        ),
                        child: Icon(
                          Icons.arrow_back,
                          color: Colors.white,
                          size: 20,
                        ),
                      ),
                    ),
                    const SizedBox(width: 16),
                    // Header Content
                    Expanded(
                      child: Column(
                        crossAxisAlignment: CrossAxisAlignment.start,
                        children: [
                          ShaderMask(
                            shaderCallback: (bounds) => LinearGradient(
                              colors: [Color(0xFFC084FC), Color(0xFFF9A8D4)],
                            ).createShader(bounds),
                            child: const Text(
                              'Kompetisi',
                              style: TextStyle(
                                fontSize: 28,
                                fontWeight: FontWeight.bold,
                                color: Colors.white,
                              ),
                            ),
                          ),
                          const SizedBox(height: 4),
                          Text(
                            'Pilih liga favoritmu',
                            style: TextStyle(
                              fontSize: 13,
                              color: Colors.grey[400],
                            ),
                          ),
                        ],
                      ),
                    ),
                  ],
                ),
              ),

              // Content
              Expanded(
                child: SingleChildScrollView(
                  padding: const EdgeInsets.all(24),
                  child: Column(
                    crossAxisAlignment: CrossAxisAlignment.start,
                    children: [
                      // Section Title
                      Row(
                        children: [
                          Icon(
                            Icons.emoji_events,
                            color: Color(0xFFC084FC),
                            size: 20,
                          ),
                          const SizedBox(width: 8),
                          const Text(
                            'Liga Eropa',
                            style: TextStyle(
                              fontSize: 20,
                              fontWeight: FontWeight.bold,
                            ),
                          ),
                        ],
                      ),
                      const SizedBox(height: 16),

                      // Description
                      Text(
                        'Jelajahi 5 liga sepak bola terbaik di Eropa. Lihat klasemen, statistik tim, top skor, dan jadwal pertandingan lengkap.',
                        style: TextStyle(
                          fontSize: 14,
                          color: Colors.grey[400],
                          height: 1.6,
                        ),
                      ),
                      const SizedBox(height: 32),

                      // Leagues List
                      ...leagues.map((league) {
                        return Padding(
                          padding: const EdgeInsets.only(bottom: 16),
                          child: buildLeagueButton(
                            context,
                            league['name'].toString(),
                            league['country'].toString(),
                            league['icon'].toString(),
                            league['colors'] as List<Color>,
                            league['id'].toString(),
                          ),
                        );
                      }).toList(),
                    ],
                  ),
                ),
              ),
            ],
          ),
        ),
      ),
    );
  }

  Widget buildLeagueButton(
    BuildContext context,
    String name,
    String country,
    String icon,
    List<Color> colors,
    String leagueId,
  ) {
    return GestureDetector(
      onTap: () {
        // Navigate to league detail page
        ScaffoldMessenger.of(context).showSnackBar(
          SnackBar(
            content: Text('Navigasi ke halaman $name'),
            duration: Duration(seconds: 1),
            backgroundColor: colors[0],
          ),
        );
      },
      child: Container(
        decoration: BoxDecoration(
          color: Colors.white.withOpacity(0.1),
          borderRadius: BorderRadius.circular(20),
          border: Border.all(
            color: Colors.white.withOpacity(0.1),
            width: 1,
          ),
        ),
        child: Stack(
          children: [
            // Hover overlay (simulated with opacity)
            Positioned.fill(
              child: Container(
                decoration: BoxDecoration(
                  gradient: LinearGradient(
                    begin: Alignment.topLeft,
                    end: Alignment.bottomRight,
                    colors: [
                      colors[0].withOpacity(0.0),
                      colors[1].withOpacity(0.0),
                    ],
                  ),
                  borderRadius: BorderRadius.circular(20),
                ),
              ),
            ),
            // Content
            Padding(
              padding: const EdgeInsets.all(24),
              child: Row(
                children: [
                  // League Icon
                  Container(
                    width: 64,
                    height: 64,
                    decoration: BoxDecoration(
                      gradient: LinearGradient(
                        begin: Alignment.topLeft,
                        end: Alignment.bottomRight,
                        colors: colors,
                      ),
                      borderRadius: BorderRadius.circular(16),
                    ),
                    child: Center(
                      child: Text(
                        icon,
                        style: TextStyle(fontSize: 32),
                      ),
                    ),
                  ),
                  const SizedBox(width: 20),

                  // League Info
                  Expanded(
                    child: Column(
                      crossAxisAlignment: CrossAxisAlignment.start,
                      children: [
                        Text(
                          name,
                          style: const TextStyle(
                            fontSize: 18,
                            fontWeight: FontWeight.w600,
                            color: Colors.white,
                          ),
                        ),
                        const SizedBox(height: 6),
                        Text(
                          country,
                          style: TextStyle(
                            fontSize: 14,
                            color: Colors.grey[400],
                          ),
                        ),
                      ],
                    ),
                  ),

                  // Arrow Icon
                  Icon(
                    Icons.chevron_right,
                    color: Color(0xFFC084FC),
                    size: 24,
                  ),
                ],
              ),
            ),
          ],
        ),
      ),
    );
  }
}
